services:
  aetherflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: aetherflow:local
    ports:
      - "8080:8080"
    env_file:
      - aetherflow.env
    volumes:
      - aetherflow-logs:/app/logs

volumes:
  aetherflow-logs:
