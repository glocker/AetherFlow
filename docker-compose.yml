services:
  aetherflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: aetherflow:local
    ports:
      - "8080:8080"
    environment:
      PORT: 8080
      AETHERFLOW_CONTROLLER_RATE_HZ: 5
    volumes:
      - aetherflow-logs:/app/logs

volumes:
  aetherflow-logs:
