services:
  aetherflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: aetherflow:local
    network_mode: host
    env_file:
      - aetherflow.env
    volumes:
      - aetherflow-logs:/app/logs

volumes:
  aetherflow-logs:
